import 'package:flutter/material.dart';

class ArtsPage extends StatelessWidget {
  const ArtsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Arts Page — Coming soon!', style: TextStyle(fontSize: 18)),
    );
  }
}
